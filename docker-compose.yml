version: "3.9"

services:
  setups-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: setups-web
    ports:
      - "8000:8000"
    volumes:
      - ./output:/app/output
      - ./cache:/app/cache
      - ./trade_data:/app/trade_data
    restart: unless-stopped

